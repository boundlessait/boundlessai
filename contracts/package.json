{
  "name": "boundless-contracts",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "compile": "mkdir -p .hardhat-home .hardhat-config && HOME=$PWD/.hardhat-home XDG_CONFIG_HOME=$PWD/.hardhat-config hardhat compile",
    "test": "mkdir -p .hardhat-home .hardhat-config && HOME=$PWD/.hardhat-home XDG_CONFIG_HOME=$PWD/.hardhat-config hardhat test",
    "clean": "mkdir -p .hardhat-home .hardhat-config && HOME=$PWD/.hardhat-home XDG_CONFIG_HOME=$PWD/.hardhat-config hardhat clean",
    "deploy:testnet": "mkdir -p .hardhat-home .hardhat-config && HOME=$PWD/.hardhat-home XDG_CONFIG_HOME=$PWD/.hardhat-config hardhat run scripts/deploy.ts --network kiteTestnet",
    "deploy:mainnet": "mkdir -p .hardhat-home .hardhat-config && HOME=$PWD/.hardhat-home XDG_CONFIG_HOME=$PWD/.hardhat-config hardhat run scripts/deploy.ts --network kite",
    "deploy:guard:testnet": "mkdir -p .hardhat-home .hardhat-config && HOME=$PWD/.hardhat-home XDG_CONFIG_HOME=$PWD/.hardhat-config hardhat run scripts/deploy-hard-guard.ts --network kiteTestnet",
    "deploy:guard:mainnet": "mkdir -p .hardhat-home .hardhat-config && HOME=$PWD/.hardhat-home XDG_CONFIG_HOME=$PWD/.hardhat-config hardhat run scripts/deploy-hard-guard.ts --network kite"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.24.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
